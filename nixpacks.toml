# Nixpacks configuration for Frontend (React + Vite)
# Place this file in the root directory as nixpacks.toml

[phases.setup]
nixPkgs = ['nodejs-20_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
# Serve the built files using a simple static server
cmd = 'npx serve -s dist -l ${PORT:-3000}'

[variables]
NODE_ENV = 'production'

